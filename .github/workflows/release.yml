name: Release

# Build, sign, notarize, and publish Hidden Bar Revived when a `v*` tag is
# pushed (e.g. `v2.0.6`). Uploads the signed + notarized zip as a release
# asset and prints the sha256 for the Homebrew cask.
#
# Repository secrets required (Settings → Secrets and variables → Actions):
#
#   MACOS_CERTIFICATE          — Developer ID Application cert + private
#                                key exported as .p12 and base64-encoded:
#                                  base64 -i DeveloperID.p12 | pbcopy
#   MACOS_CERTIFICATE_PASSWORD — password protecting the .p12
#   KEYCHAIN_PASSWORD          — any value used for the throwaway
#                                CI keychain created per run
#   APPLE_ID                   — Apple ID email tied to the developer account
#   APPLE_TEAM_ID              — 485WH9DHS4
#   APPLE_APP_PASSWORD         — app-specific password generated at appleid.apple.com

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to release (e.g. v2.0.6) — must already exist"
        required: true

permissions:
  contents: write

jobs:
  release:
    name: Build and publish
    # macos-latest tracks the newest GitHub-hosted macOS image so CI builds
    # with Xcode 26+ / macOS 26 SDK — required by Apple for any future App
    # Store Connect submission, and harmless for Developer ID distribution.
    runs-on: macos-latest

    env:
      SIGNING_IDENTITY: "Developer ID Application: Shelby Denike (485WH9DHS4)"
      NOTARY_PROFILE: "HiddenBarNotary"
      APP_NAME: "Hidden Bar Revived"
      SCHEME: "Hidden Bar"
      PROJECT: "Hidden Bar.xcodeproj"
      MIN_XCODE_MAJOR: "26"
      MIN_SDK_MAJOR: "26"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}
          fetch-depth: 0

      - name: Verify Xcode and SDK version
        run: |
          # awk 'NR==1' instead of '| head -1' — head exits after one line
          # and triggers SIGPIPE on the upstream xcodebuild, which combined
          # with `set -euo pipefail` (default in GitHub Actions run steps)
          # silently kills the step with exit 141.
          XCODE_VERSION="$(xcodebuild -version | awk 'NR==1 { print $2 }')"
          XCODE_MAJOR="${XCODE_VERSION%%.*}"
          SDK_VERSION="$(xcrun --sdk macosx --show-sdk-version)"
          SDK_MAJOR="${SDK_VERSION%%.*}"

          echo "Xcode: $XCODE_VERSION (major $XCODE_MAJOR)"
          echo "macOS SDK: $SDK_VERSION (major $SDK_MAJOR)"

          if [[ "$XCODE_MAJOR" -lt "$MIN_XCODE_MAJOR" ]]; then
              echo "::error::Xcode $XCODE_VERSION is older than required Xcode $MIN_XCODE_MAJOR. Apple requires Xcode 26+ for App Store Connect submissions starting 2026-04-28."
              exit 1
          fi
          if [[ "$SDK_MAJOR" -lt "$MIN_SDK_MAJOR" ]]; then
              echo "::error::macOS SDK $SDK_VERSION is older than required SDK $MIN_SDK_MAJOR. Build would not satisfy Apple's App Store Connect SDK requirement."
              exit 1
          fi

      - name: Resolve version
        id: version
        run: |
          VERSION="$(grep -m1 'MARKETING_VERSION = ' "$PROJECT/project.pbxproj" | sed -E 's/.*= *([^;]+);.*/\1/' | tr -d ' ')"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Releasing v$VERSION"

      - name: Import signing certificate
        env:
          MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
          MACOS_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          KEYCHAIN="$RUNNER_TEMP/build.keychain-db"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security set-keychain-settings -lut 21600 "$KEYCHAIN"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"

          CERT_PATH="$RUNNER_TEMP/cert.p12"
          echo -n "$MACOS_CERTIFICATE" | base64 --decode -o "$CERT_PATH"
          security import "$CERT_PATH" -P "$MACOS_CERTIFICATE_PASSWORD" \
              -A -t cert -f pkcs12 -k "$KEYCHAIN"
          security list-keychain -d user -s "$KEYCHAIN" login.keychain
          security set-key-partition-list -S apple-tool:,apple: \
              -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          rm -f "$CERT_PATH"

      - name: Store notarization credentials
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
        run: |
          xcrun notarytool store-credentials "$NOTARY_PROFILE" \
              --apple-id "$APPLE_ID" \
              --team-id "$APPLE_TEAM_ID" \
              --password "$APPLE_APP_PASSWORD"

      - name: Build, sign, notarize, staple, package
        run: ./scripts/release.sh

      - name: Upload release asset
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ steps.version.outputs.version }}
          TAG_INPUT: ${{ github.event.inputs.tag }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          TAG="${TAG_INPUT:-$REF_NAME}"
          ARTIFACT="dist/HiddenBarRevived-$VERSION.zip"

          if ! gh release view "$TAG" >/dev/null 2>&1; then
              gh release create "$TAG" \
                  --title "v$VERSION" \
                  --notes-file CHANGELOG.md \
                  "$ARTIFACT"
          else
              gh release upload "$TAG" "$ARTIFACT" --clobber
          fi

          SHA="$(/usr/bin/shasum -a 256 "$ARTIFACT" | awk '{ print $1 }')"
          {
              echo "## Cask values"
              echo '```ruby'
              echo "version \"$VERSION\""
              echo "sha256 \"$SHA\""
              echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      # Keep sdenike/homebrew-tap's cask in step with the release just published.
      # Commits straight to the tap's default branch rather than opening a pull
      # request: a PR per release would need merging every time, which is the
      # manual step this exists to remove. The tap's commit history is the record.
      #
      # Needs HOMEBREW_TAP_TOKEN — a PAT with contents:write on sdenike/homebrew-tap.
      # The workflow's own GITHUB_TOKEN cannot write to another repository.
      - name: Check for tap token
        id: taptoken
        env:
          TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
        run: |
          set -euo pipefail
          if [ -n "${TAP_TOKEN:-}" ]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
            echo "::warning::HOMEBREW_TAP_TOKEN is not set — skipping the Homebrew cask update. The release itself is unaffected."
          fi

      - name: Check out the Homebrew tap
        if: steps.taptoken.outputs.present == 'true'
        uses: actions/checkout@v4
        with:
          repository: sdenike/homebrew-tap
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          path: homebrew-tap

      - name: Bump hidden-revived cask
        if: steps.taptoken.outputs.present == 'true'
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          set -euo pipefail
          ARTIFACT="dist/HiddenBarRevived-${VERSION}.zip"
          SHA=$(/usr/bin/shasum -a 256 "$ARTIFACT" | awk '{ print $1 }')
          CASK="homebrew-tap/Casks/hidden-revived.rb"
          [ -f "$CASK" ] || { echo "::error::$CASK not found in the tap"; exit 1; }

          /usr/bin/sed -i '' -E "s|^  version \".*\"$|  version \"${VERSION}\"|" "$CASK"
          /usr/bin/sed -i '' -E "s|^  sha256 \".*\"$|  sha256 \"${SHA}\"|" "$CASK"

          grep -q "version \"${VERSION}\"" "$CASK" || { echo "::error::version substitution failed"; exit 1; }
          grep -q "sha256 \"${SHA}\"" "$CASK"      || { echo "::error::sha256 substitution failed"; exit 1; }

          cd homebrew-tap
          if git diff --quiet; then
            echo "Cask already at ${VERSION}; nothing to do."
            exit 0
          fi
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -am "hidden-revived ${VERSION}

          Released by sdenike/hidden-revived's release workflow, run ${GITHUB_RUN_ID}."
          git push
